'use client'
import Link from 'next/link'
import {useEffect,useState} from 'react'
import {createClient} from '../../../lib/supabase/client'
type Child={id:string;first_name:string;last_name:string;admission_number:string;portal_code:string|null;classes?:{name?:string}|null;streams?:{name?:string}|null}
const modules=[['Results','View published exam results and report cards.','/dashboard/reports'],['Attendance','Track attendance and important attendance notices.','/dashboard/attendance'],['Fees & Receipts','See balances, payments and available receipts.','/dashboard/finance'],['Announcements','Read school notices and parent communications.','/dashboard/communications'],['Assignments','Stay updated with learning activities and homework.','/dashboard/timetable'],['Transport','View your child’s assigned school transport information.','/dashboard/transport']]
export default function ParentDashboard(){const[children,setChildren]=useState<Child[]>([]),[parentName,setParentName]=useState('Parent'),[message,setMessage]=useState('Loading your account…');useEffect(()=>{async function load(){const db=createClient();const{data:auth}=await db.auth.getUser();if(!auth.user){window.location.href='/parent-portal';return}const{data:parent}=await db.from('parents').select('id,name').eq('profile_id',auth.user.id).maybeSingle();if(!parent){setMessage('Your parent account is signed in, but it is not yet linked to a school parent record. Contact the school office.');return}setParentName(parent.name);const{data,error}=await db.from('student_parents').select('students(id,first_name,last_name,admission_number,portal_code,classes(name),streams(name))').eq('parent_id',parent.id);if(error){setMessage(error.message);return}const linked:Child[]=(data??[]).flatMap((row:any)=>{const raw=row.students;const student=Array.isArray(raw)?raw[0]:raw;if(!student)return[];const classes=Array.isArray(student.classes)?student.classes[0]??null:student.classes??null;const streams=Array.isArray(student.streams)?student.streams[0]??null:student.streams??null;return[{id:student.id,first_name:student.first_name,last_name:student.last_name,admission_number:student.admission_number,portal_code:student.portal_code,classes,streams}]});setChildren(linked);setMessage(linked.length?'':'No learners are currently linked to this parent account.')}void load()},[]);async function signOut(){await createClient().auth.signOut();window.location.href='/parent-portal'}return <main className="parent-dashboard"><header><Link href="/" className="brand"><img src="/aic-cathedral-logo.svg" alt="AIC Cathedral logo"/><span><b>AIC CATHEDRAL</b><small>PARENT PORTAL</small></span></Link><div><span className="status">Secure parent account</span><button className="secondary" onClick={signOut}>Sign out</button></div></header><section className="welcome"><div><div className="eyebrow">PARENT PORTAL</div><h1>Welcome, {parentName}.</h1><p>Everything you need to stay connected with your child’s school journey.</p></div><div className="student-pill"><span>Linked learners</span><b>{children.length}</b><small>Live information from the school system</small></div></section>{message&&<div className="notice">{message}</div>}<section className="children"><h2>Your learners</h2><div className="child-list">{children.map(child=><article key={child.id}><div><strong>{child.first_name} {child.last_name}</strong><span>Admission: {child.admission_number}</span><span>{child.classes?.name||'Grade not assigned'} · {child.streams?.name||'Stream not assigned'}</span></div><b>{child.portal_code||'Portal code pending'}</b></article>)}</div></section><section className="parent-grid">{modules.map(([title,desc,href])=><Link href={href} key={title} style={{textDecoration:'none'}}><article><div className="icon">◆</div><h2>{title}</h2><p>{desc}</p><span>Open →</span></article></Link>)}</section><footer><Link href="/">AIC Cathedral Primary School</Link><span>Education for Excellence · Created by DIGITAL KINGDOM MEDIA</span></footer></main>}
