'use client'
import {FormEvent,useState} from 'react';import{useRouter}from'next/navigation';import{createClient}from'../../lib/supabase/client'
export default function Register(){const r=useRouter(),[email,setEmail]=useState('admin@aiccathedral.ac.ke'),[password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[error,setError]=useState(''),[message,setMessage]=useState(''),[loading,setLoading]=useState(false);
async function submit(e:FormEvent){e.preventDefault();setError('');setMessage('');if(email.trim().toLowerCase()!=='admin@aiccathedral.ac.ke'){setError('Only the school admin account can be created here.');return}if(password.length<8){setError('Use at least 8 characters for the new password/PIN.');return}if(password!==confirm){setError('The passwords do not match.');return}setLoading(true);try{const s=createClient();const{data,error}=await s.auth.signUp({email:email.trim().toLowerCase(),password});if(error)throw error;if(data.session)r.replace('/dashboard');else setMessage('Account created. Check the admin email for confirmation, then return to Sign in.')}catch(err:any){setError(err?.message||'Unable to create the admin account.')}finally{setLoading(false)}}
return <main className="login-shell"><section className="login-card"><div className="brand"><div className="logo">AIC</div><div><strong>AIC Cathedral</strong><div className="muted">Primary School ERP</div></div></div><h1>Set up admin account</h1><p className="muted">Create the school admin account once, using a password/PIN you choose.</p><form onSubmit={submit}><label>Admin email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email"/><label>New password / PIN</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={8} autoComplete="new-password"/><label>Confirm password / PIN</label><input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} required minLength={8} autoComplete="new-password"/><p style={{minHeight:18,color:error?'#b42318':'#18794e',fontSize:13}}>{error||message}</p><button className="btn" disabled={loading}>{loading?'Creating…':'Create admin account'}</button></form><p className="muted" style={{marginTop:16,fontSize:13}}>Already created? <a href="/login">Sign in</a></p></section></main>}
