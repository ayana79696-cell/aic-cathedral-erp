import { createClient } from '../../../lib/supabase/server'
import { addStudent } from './actions'
export default async function Students(){const s=await createClient();const {data}=await s.from('students').select('id,admission_number,first_name,last_name,status').order('last_name');return <main className="main"><header className="top"><div><h1>Students</h1><p className="muted">Admissions and learner records</p></div></header><section className="card"><form action={addStudent} style={{display:'grid',gridTemplateColumns:'1fr 1fr 1fr auto',gap:10,marginBottom:20}}><input name="admission_number" placeholder="Admission No." required/><input name="first_name" placeholder="First name" required/><input name="last_name" placeholder="Last name" required/><button className="btn">Add student</button></form><div style={{overflowX:'auto'}}><table style={{width:'100%',borderCollapse:'collapse'}}><thead><tr><th style={{textAlign:'left',padding:12}}>Admission No.</th><th style={{textAlign:'left',padding:12}}>Student</th><th style={{textAlign:'left',padding:12}}>Status</th></tr></thead><tbody>{data?.length?data.map(x=><tr key={x.id}><td style={{padding:12}}>{x.admission_number}</td><td style={{padding:12}}>{x.first_name} {x.last_name}</td><td style={{padding:12}}>{x.status}</td></tr>):<tr><td colSpan={3} style={{padding:35,textAlign:'center'}} className="muted">No students yet.</td></tr>}</tbody></table></div></section></main>}