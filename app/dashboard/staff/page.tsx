import Link from 'next/link'
import SimpleCrud from '../_components/simple-crud'
import {createClient} from '../../../lib/supabase/server'
export default async function Page(){const s=await createClient();const{data}=await s.from('teacher_assignments').select('id,teacher_id,class_id,stream_id,learning_area_id,staff:staff(first_name,last_name),classes:classes(name),streams:streams(name),learning_areas:learning_areas(name)').order('id');return <main className="main"><header className="top"><div><h1>Staff & Teachers</h1><p className="muted">Employees, teachers, class/stream assignments and location attendance.</p></div><Link className="site-btn" href="/dashboard/staff/checkin">Teacher check-in</Link></header><SimpleCrud table="staff" title="Staff directory" fields={[{name:'employee_number',label:'Employee No.',required:true},{name:'first_name',label:'First name',required:true},{name:'last_name',label:'Last name',required:true},{name:'phone',label:'Phone'},{name:'email',label:'Email',type:'email'},{name:'department',label:'Department'},{name:'job_title',label:'Job title'},{name:'employment_type',label:'Employment type',options:['permanent','contract','casual','intern']}]} /><section className="card"><h2>Teacher assignments</h2><p className="muted">Assign class teachers and subject teachers to grades, streams and CBC learning areas.</p><div style={{overflowX:'auto'}}><table style={{width:'100%',borderCollapse:'collapse'}}><thead><tr><th style={{textAlign:'left',padding:10}}>Teacher</th><th style={{textAlign:'left',padding:10}}>Class</th><th style={{textAlign:'left',padding:10}}>Stream</th><th style={{textAlign:'left',padding:10}}>Learning Area</th></tr></thead><tbody>{data?.length?data.map((x:any)=><tr key={x.id}><td style={{padding:10}}>{x.staff?.first_name} {x.staff?.last_name}</td><td style={{padding:10}}>{x.classes?.name||'—'}</td><td style={{padding:10}}>{x.streams?.name||'All streams'}</td><td style={{padding:10}}>{x.learning_areas?.name||'—'}</td></tr>):<tr><td colSpan={4} className="muted" style={{padding:30,textAlign:'center'}}>No teacher assignments yet.</td></tr>}</tbody></table></div></section></main>}