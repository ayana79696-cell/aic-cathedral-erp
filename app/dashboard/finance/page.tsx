import {createClient} from '../../../lib/supabase/server'
import FinanceDesk from './finance-desk'
import SimpleCrud from '../_components/simple-crud'
export default async function Page(){const s=await createClient();const [{data:students},{data:academicYear}]=await Promise.all([s.from('students').select('id,admission_number,first_name,middle_name,last_name,class_id,stream_id').eq('status','active').order('last_name'),s.from('academic_years').select('id').eq('status','active').order('year',{ascending:false}).limit(1).maybeSingle()]);const [{data:classes},{data:streams}]=await Promise.all([s.from('classes').select('id,name').eq('status','active'),s.from('streams').select('id,class_id,name').eq('status','active')]);const cm=new Map((classes||[]).map(c=>[c.id,c.name])),sm=new Map((streams||[]).map(x=>[x.id,x.name]));const fs=(students||[]).map(x=>({...x,class_name:cm.get(x.class_id||'')||'Unassigned',stream_name:sm.get(x.stream_id||'')||'Unassigned'}));const opts=fs.map(x=>({value:x.id,label:`${x.admission_number} — ${x.first_name} ${x.last_name}`}));return <main className="main"><header className="top"><div><h1>Finance & Fees</h1><p className="muted">Admission search, grade/stream grouping, fees, receipts, banks and other income.</p></div></header><FinanceDesk students={fs} academicYearId={academicYear?.id}/><SimpleCrud table="finance_banks" title="Banks used by the school" fields={[{name:'bank_name',label:'Bank',options:['Equity Bank','Co-operative Bank','KCB'],required:true},{name:'account_name',label:'Account name'},{name:'account_number',label:'Account number'},{name:'branch',label:'Branch'},{name:'active',label:'Active',type:'boolean',options:['true','false']}]} /><SimpleCrud table="fee_accounts" title="Learner fee accounts" fields={[{name:'student_id',label:'Learner',required:true,options:opts},{name:'amount_due',label:'Amount due (KES)',type:'number',required:true},{name:'amount_paid',label:'Amount paid (KES)',type:'number'},{name:'status',label:'Status',options:['unpaid','partial','paid','waived']}]} /><SimpleCrud table="fee_payments" title="Fees collected & receipts" fields={[{name:'student_id',label:'Learner',required:true,options:opts},{name:'amount',label:'Amount (KES)',type:'number',required:true},{name:'payment_method',label:'Method',options:['cash','mpesa','bank','card','other']},{name:'reference_no',label:'Reference / receipt no.'}]} /><SimpleCrud table="other_income" title="Other income" fields={[{name:'income_date',label:'Date',type:'date'},{name:'source',label:'Source',required:true},{name:'description',label:'Description'},{name:'amount',label:'Amount (KES)',type:'number',required:true},{name:'reference_no',label:'Reference'}]} /></main>}