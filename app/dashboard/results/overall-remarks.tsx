'use client'
import{useEffect,useState}from'react'
import{createClient}from'../../../lib/supabase/client'
type P={role:string;students:any[];exams:any[];classes:any[];streams:any[]}
export default function OverallRemarks({role,students,exams,classes,streams}:P){const s=createClient();const[exam,setExam]=useState(''),[cls,setCls]=useState(''),[stream,setStream]=useState(''),[student,setStudent]=useState(''),[comment,setComment]=useState(''),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);const list=students.filter(x=>x.class_id===cls&&(!stream||x.stream_id===stream));useEffect(()=>{setStudent('');setComment('')},[exam,cls,stream]);useEffect(()=>{if(!student||!exam)return; s.from('result_remarks').select('class_teacher_remark,headteacher_remark').eq('student_id',student).eq('exam_id',exam).maybeSingle().then((res:any)=>{const d=res.data;setComment(role==='class_teacher'?d?.class_teacher_remark||'':d?.headteacher_remark||'')})},[student,exam,role]);const save=async()=>{setBusy(true);setMsg('');const uid=(await s.auth.getUser()).data.user?.id;if(!uid||!student||!exam||!cls){setMsg('Select exam, class and learner first.');setBusy(false);return}const p:any={student_id:student,exam_id:exam,class_id:cls,stream_id:stream||null};if(role==='class_teacher'){p.class_teacher_remark=comment;p.class_teacher_id=uid}else{p.headteacher_remark=comment;p.headteacher_id=uid}const{error}=await s.from('result_remarks').upsert(p,{onConflict:'student_id,exam_id'});setMsg(error?'Could not save: '+error.message:'Overall comment saved successfully.');setBusy(false)};if(!['class_teacher','headteacher'].includes(role))return null;return <section className="card"><h2>{role==='class_teacher'?'Class Teacher Comment':'Headteacher Comment'}</h2><p className="muted">Only one overall comment is recorded here. Individual learning-area remarks are not used.</p><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(180px,1fr))',gap:8,marginTop:12}}><select value={exam} onChange={e=>setExam(e.target.value)}><option value="">Select exam</option>{exams.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select value={cls} onChange={e=>{setCls(e.target.value);setStream('')}}><option value="">Select class</option>{classes.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select value={stream} onChange={e=>setStream(e.target.value)} disabled={!cls}><option value="">All streams</option>{streams.filter(x=>x.class_id===cls).map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select value={student} onChange={e=>setStudent(e.target.value)} disabled={!cls}><option value="">Select learner</option>{list.map(x=><option key={x.id} value={x.id}>{x.admission_number} — {[x.first_name,x.middle_name,x.last_name].filter(Boolean).join(' ')}</option>)}</select></div><textarea value={comment} onChange={e=>setComment(e.target.value)} placeholder={role==='class_teacher'?'Enter overall class teacher comment...':'Enter overall headteacher comment...'} rows={4} style={{width:'100%',marginTop:12,padding:10,borderRadius:8,border:'1px solid #d0d5dd'}}/><button className="btn btn-primary" onClick={save} disabled={busy} style={{marginTop:10}}>{busy?'Saving…':'Save comment'}</button>{msg&&<p className="muted">{msg}</p>}</section>}