import SimpleCrud from '../_components/simple-crud'
export default function Page(){return <main className="main"><header className="top"><div><h1>Attendance</h1><p className="muted">Record daily learner attendance and review history.</p></div></header><SimpleCrud table="attendance_records" title="Learner attendance" fields={[{name:'student_id',label:'Student ID',required:true},{name:'attendance_date',label:'Date',type:'date',required:true},{name:'status',label:'Status',options:['present','absent','late','excused']},{name:'note',label:'Note'}]} /></main>}