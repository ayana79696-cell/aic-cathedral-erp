import {createClient} from '../../../lib/supabase/server'
export default async function Audit(){const s=await createClient();const{data}=await s.from('audit_logs').select('*').order('created_at',{ascending:false}).limit(100);return <main className="main"><header className="top"><div><h1>Audit Logs</h1><p className="muted">Security and important system activity.</p></div></header><section className="card"><div style={{overflowX:'auto'}}><table style={{width:'100%',borderCollapse:'collapse'}}><thead><tr><th style={{textAlign:'left',padding:10}}>Time</th><th style={{textAlign:'left',padding:10}}>Action</th><th style={{textAlign:'left',padding:10}}>Table</th><th style={{textAlign:'left',padding:10}}>Record</th></tr></thead><tbody>{data?.length?data.map((x:any)=><tr key={x.id}><td style={{padding:10}}>{new Date(x.created_at).toLocaleString()}</td><td style={{padding:10}}>{x.action}</td><td style={{padding:10}}>{x.table_name||'—'}</td><td style={{padding:10}}>{x.record_id||'—'}</td></tr>):<tr><td colSpan={4} className="muted" style={{padding:30,textAlign:'center'}}>No audit events yet.</td></tr>}</tbody></table></div></section></main>}