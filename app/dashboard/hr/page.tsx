import Link from'next/link';import{createClient}from'../../../lib/supabase/server'
export default async function Page(){const s=await createClient();const{data}=await s.from('staff_attendance').select('id,staff_user_id,attendance_date,check_type,checked_at,accuracy_meters,latitude,longitude').order('checked_at',{ascending:false}).limit(100);return <main className="main"><header className="top"><div><h1>HR & Payroll</h1><p className="muted">Staff records, attendance, leave and payroll administration.</p></div></header><section className="card"><h2>Staff location attendance</h2><p className="muted">Recent GPS check-in and check-out events.</p><div style={{overflowX:'auto'}}><table style={{width:'100%',borderCollapse:'collapse'}}><thead><tr><th style={{textAlign:'left',padding:10}}>Date</th><th style={{textAlign:'left',padding:10}}>Type</th><th style={{textAlign:'left',padding:10}}>Time</th><th style={{textAlign:'left',padding:10}}>Accuracy</th><th style={{textAlign:'left',padding:10}}>Location</th></tr></thead><tbody>{data?.length?data.map((x:any)=><tr key={x.id}><td style={{padding:10}}>{x.attendance_date}</td><td style={{padding:10}}>{x.check_type}</td><td style={{padding:10}}>{new Date(x.checked_at).toLocaleString()}</td><td style={{padding:10}}>{x.accuracy_meters?`${Math.round(x.accuracy_meters)} m`:'—'}</td><td style={{padding:10}}>{x.latitude&&x.longitude?`${Number(x.latitude).toFixed(5)}, ${Number(x.longitude).toFixed(5)}`:'—'}</td></tr>):<tr><td colSpan={5} className="muted" style={{padding:30,textAlign:'center'}}>No attendance events yet.</td></tr>}</tbody></table></div></section><section className="card" style={{marginTop:16}}><h2>Staff tools</h2><Link className="btn" href="/dashboard/staff/checkin">Open staff check-in</Link></section></main>}