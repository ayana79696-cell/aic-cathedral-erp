'use client'
import {useState} from 'react'
import {useRouter} from 'next/navigation'
import {createClient} from '../../lib/supabase/client'
export default function TeacherLogin(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false),router=useRouter();async function submit(e:any){e.preventDefault();setBusy(true);setError('');const{error}=await createClient().auth.signInWithPassword({email,password});if(error)setError(error.message);else router.push('/dashboard');setBusy(false)}return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:20,background:'#f4f7fb'}}><form onSubmit={submit} className="card" style={{width:'100%',maxWidth:430}}><img src="/aic-cathedral-logo.svg" alt="AIC Cathedral Primary School" style={{width:150,height:'auto',marginBottom:18}}/><h1>Teacher Login</h1><p className="muted">Sign in to access your classes, results, attendance and timetable.</p><label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required placeholder="Teacher email"/><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required placeholder="Password"/><button className="btn" style={{width:'100%',marginTop:16}} disabled={busy}>{busy?'Signing in…':'Sign in as teacher'}</button>{error&&<p style={{color:'#b42318'}}>{error}</p>}</form></main>}
