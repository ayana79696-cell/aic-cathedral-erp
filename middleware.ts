import { createServerClient } from '@supabase/ssr'
import { NextResponse, type NextRequest } from 'next/server'
const access:Record<string,string[]|undefined>={'/dashboard/students':['super_admin','headteacher','deputy_headteacher','class_teacher','subject_teacher'],'/dashboard/academic':['super_admin','headteacher','deputy_headteacher'],'/dashboard/exams':['super_admin','headteacher','deputy_headteacher','class_teacher','subject_teacher'],'/dashboard/results':['super_admin','headteacher','deputy_headteacher','class_teacher','subject_teacher'],'/dashboard/attendance':['super_admin','headteacher','deputy_headteacher','class_teacher','subject_teacher'],'/dashboard/finance':['super_admin','headteacher','accountant'],'/dashboard/staff':['super_admin','headteacher','deputy_headteacher','hr_admin'],'/dashboard/hr':['super_admin','headteacher','hr_admin'],'/dashboard/procurement':['super_admin','headteacher','procurement_officer','storekeeper'],'/dashboard/inventory':['super_admin','headteacher','procurement_officer','storekeeper'],'/dashboard/transport':['super_admin','headteacher','transport_manager'],'/dashboard/communications':['super_admin','headteacher','deputy_headteacher'],'/dashboard/reports':['super_admin','headteacher','deputy_headteacher','accountant','hr_admin','procurement_officer','storekeeper','transport_manager','class_teacher','subject_teacher'],'/dashboard/users':['super_admin'],'/dashboard/audit':['super_admin']}
export async function middleware(request:NextRequest){let response=NextResponse.next({request});const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key)return response;const s=createServerClient(url,key,{cookies:{getAll(){return request.cookies.getAll()},setAll(c){c.forEach(({name,value,options})=>response.cookies.set(name,value,options))}}});const{data:{user}}=await s.auth.getUser();const path=request.nextUrl.pathname;if(path==='/' )return response;if(path==='/login'&&user)return NextResponse.redirect(new URL('/dashboard',request.url));if(path==='/change-password'&&!user)return NextResponse.redirect(new URL('/login',request.url));if(path.startsWith('/dashboard')&&!user)return NextResponse.redirect(new URL('/login',request.url));if(user&&path.startsWith('/dashboard')){const{data:p}=await s.from('profiles').select('role,must_change_password').eq('id',user.id).maybeSingle();if(p?.must_change_password)return NextResponse.redirect(new URL('/change-password',request.url));const base=Object.keys(access).find(x=>path===x||path.startsWith(x+'/'));if(base&&p?.role!=='super_admin'&&!access[base]?.includes(p?.role||''))return NextResponse.redirect(new URL('/dashboard',request.url))}return response}
export const config={matcher:['/dashboard/:path*','/login','/change-password','/']}